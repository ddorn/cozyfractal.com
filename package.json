{
  "dependencies": {
    "vue": "^3.0.5"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0"
  },
  "scripts": {
    "tw": "tailwind build static/source.css -o static/style.css -c tailwind.config.js --no-autoprefixer",
    "tw-prod": "NODE_ENV=production npm run tw",
    "start": "poetry run uvicorn app:app --port ${PORT:-8400}"
  }
}
